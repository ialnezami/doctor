---
phase: "5.2"
plan: "02"
subsystem: "User Model / GDPR Compliance"
tags: [gdpr, hipaa, consent-tracking, blind-index, erasure, mongodb]
dependency_graph:
  requires: []
  provides:
    - User.emailHash field + unique sparse index
    - User consent fields (consentVersion, consentTimestamp, consentIp, consentWithdrawnAt, dataProcessingAllowed)
    - User.erasedAt field
  affects:
    - apps/api/src/models/User.js
    - apps/api/src/routes/auth.js (Plan 05 will switch login to use emailHash)
    - apps/api/src/middleware/auth.js (Plan 06 will check erasedAt)
tech_stack:
  added: []
  patterns:
    - HMAC-SHA256 blind index on email field (searchable without revealing plaintext)
    - GDPR Article 7 consent tracking with IP, timestamp, version, and withdrawal
    - GDPR Article 17 erasure flag for JWT invalidation
key_files:
  modified:
    - apps/api/src/models/User.js
decisions:
  - "emailHash sparse index allows null during migration (Google-only users or pre-hook saves)"
  - "dataProcessingAllowed defaults to false — consent must be given explicitly at registration (Plan 05)"
  - "Two separate pre-save hooks (bcrypt password + emailHash) instead of merging to follow Single Responsibility"
  - "hmacHash imported at module top-level; missing BLIND_INDEX_KEY fails loudly on save, not silently"
metrics:
  duration: "5 minutes"
  completed: "2026-06-29"
  tasks_completed: 1
  tasks_total: 1
  files_modified: 1
---

# Phase 5.2 Plan 02: User Model GDPR Compliance Fields Summary

User.js updated with HMAC-SHA256 email blind index, full GDPR Article 7 consent tracking, and Article 17 erasure flag — enabling login-by-hash (Plan 05) and token invalidation post-erasure (Plan 06) without modifying this file again.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add emailHash, consent fields, and erasedAt to User schema | ea2394e | apps/api/src/models/User.js |

## Deviations from Plan

None — plan executed exactly as written.

## Verification Results

All acceptance criteria passed:
- `emailHash` appears 5 times (field def, index definition, pre-save hook require, hook call, comment)
- All consent fields present with correct defaults (null/false — never auto-true)
- `sparse: true` appears twice (existing googleId + new emailHash)
- `bcrypt` count 3 — existing hook and comparePassword preserved
- `hmacHash` count 2 — require import + hook call

## Known Stubs

None — no data wiring stubs. `emailHash` will be null on existing documents until re-saved; Plan 05 must handle migration or accept null during transition.

## Threat Flags

| Flag | File | Description |
|------|------|-------------|
| threat_flag: pii_at_rest | apps/api/src/models/User.js | consentIp stores an IP address (PII under GDPR); encrypt in Plan 03 if field-encryption scope includes it |

## Self-Check: PASSED

- File exists: apps/api/src/models/User.js — FOUND
- Commit ea2394e exists in git log — FOUND
- All grep checks passed (see Verification Results above)
