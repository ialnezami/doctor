---
phase: 09-ai-patient-chatbot
plan: 4
type: execute
wave: 4
depends_on: [09.1, 09.2, 09.3]
files_modified:
  - apps/api/src/routes/chatbot.js
  - apps/api/src/services/chatbotService.js
  - apps/api/src/utils/sessionStore.js
  - apps/api/src/routes/__tests__/chatbot.test.js
  - apps/api/src/utils/__tests__/sessionStore.test.js
  - apps/api/src/utils/__tests__/triageParser.test.js
  - apps/api/src/utils/__tests__/doctorRanking.test.js
  - .planning/phases/09-ai-patient-chatbot/HIPAA-AUDIT.md
  - .planning/phases/09-ai-patient-chatbot/BAA-STATUS.md
  - .planning/phases/09-ai-patient-chatbot/QA-CHECKLIST.md
autonomous: false
requirements:
  - P9-R5
  - P9-R6

must_haves:
  truths:
    - "End-to-end smoke test succeeds on staging (mobile → API → Anthropic → doctors) with real JWT"
    - "Rate limit enforcement verified with automated load: 31 requests same user in <1hr → 429 on 31st"
    - "HIPAA audit document exists and lists all PHI touchpoints with mitigations"
    - "Zero PHI in any log source: server console, files, external services"
    - "Full Jest suite for chatbot modules passes: chatbot, sessionStore, triageParser, doctorRanking"
    - "AbortController timeout verified: mock 30s Anthropic response → SSE error event within 26s"
    - "BAA-STATUS.md is either Status: SIGNED or explicitly flagged as production blocker"
    - "QA checklist file records manual verification results per platform (mobile + web)"
    - "Session TTL verified: send message, wait 31 min, next message starts fresh session"
    - "Emergency urgency short-circuit tested with real prompt on both mobile + web"
  artifacts:
    - path: ".planning/phases/09-ai-patient-chatbot/HIPAA-AUDIT.md"
      provides: "Recorded PHI touchpoint inventory, mitigations, verification steps"
    - path: ".planning/phases/09-ai-patient-chatbot/QA-CHECKLIST.md"
      provides: "Manual QA results log for mobile + web smoke tests"
    - path: "apps/api/src/routes/__tests__/chatbot.test.js"
      provides: "Full integration tests (including rate limit, emergency, session reset)"
  key_links:
    - from: "apps/api/src/routes/chatbot.js"
      to: ".planning/phases/09-ai-patient-chatbot/HIPAA-AUDIT.md"
      via: "Code changes verified against audit checklist"
      pattern: "HIPAA|PHI"
    - from: ".planning/phases/09-ai-patient-chatbot/BAA-STATUS.md"
      to: "Production deployment gate"
      via: "Status field must read SIGNED"
      pattern: "Status:.*SIGNED"
---

<objective>
Wave 4 hardens Phase 9 for production: complete integration testing, HIPAA audit sign-off, load-test rate limiting, verify timeout handling, verify no-PHI-logging invariant, close BAA gate, and produce a QA checklist that documents cross-platform verification.

Purpose: Waves 1-3 built the feature. Wave 4 proves it is safe to run in production. Healthcare features carry regulatory + reputational risk; we cannot ship on "worked in dev" alone. Every threat identified in Waves 1-3 must have a corresponding verification here.

Output: A signed-off Phase 9 ready for production release, with all audit artifacts committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ai-patient-chatbot/09-RESEARCH.md
@.planning/phases/09-ai-patient-chatbot/09.1-PLAN.md
@.planning/phases/09-ai-patient-chatbot/09.2-PLAN.md
@.planning/phases/09-ai-patient-chatbot/09.3-PLAN.md
@.planning/phases/09-ai-patient-chatbot/BAA-STATUS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete integration test suite (rate limit, emergency, timeout, session TTL)</name>
  <files>apps/api/src/routes/__tests__/chatbot.test.js, apps/api/src/utils/__tests__/sessionStore.test.js, apps/api/src/utils/__tests__/triageParser.test.js, apps/api/src/utils/__tests__/doctorRanking.test.js</files>
  <read_first>
    - apps/api/src/routes/__tests__/chatbot.test.js (existing scaffolding from 09.1 Task 1)
    - apps/api/src/routes/chatbot.js (module under test)
    - apps/api/src/services/chatbotService.js
    - apps/api/src/utils/sessionStore.js
    - apps/api/src/workers/__tests__/symptomWorker.test.js (existing Anthropic mock pattern)
    - .planning/phases/09-ai-patient-chatbot/09-RESEARCH.md#validation-architecture
  </read_first>
  <behavior>
    - chatbot.test.js expanded to cover:
      * REQ 9.1-A: POST /message with doctor JWT → 403
      * REQ 9.1-A: POST /message with no JWT → 401
      * REQ 9.1-B: POST /message empty body → 422
      * REQ 9.1-B: message > 2000 chars → 422
      * REQ 9.1-B: lat=95 (out of range) → 422
      * REQ 9.1-C: 31 rapid requests from same user → 31st returns 429
      * REQ 9.1-E: mocked Anthropic returns triage with urgency=emergency → response `done` event has doctors=[], emergency=true (assert via captured res.write calls)
      * REQ 9.4-A: DELETE /session → 204, subsequent snapshotHistory returns []
      * Anthropic 30s timeout → AbortController fires, error event emitted, connection closes (mock stream that never yields)
    - sessionStore.test.js: TTL sliding window (verify with fake timers); MAX_TURNS cap; snapshotHistory isolation
    - triageParser.test.js: all 4 urgency values accepted; unknown urgency rejected; malformed JSON returns null; missing block returns null
    - doctorRanking.test.js (skip if no test MongoDB): fallback triggers on <3 results; lat/lng validation; score ordering
  </behavior>
  <action>
    1. Mock `@anthropic-ai/sdk` with `jest.mock('@anthropic-ai/sdk', () => ({ default: jest.fn(...) }))` OR use `jest.mock` with a factory that returns a fake `.messages.stream()` returning an async iterable.
    2. Mock `express-rate-limit` sparingly — the actual limiter is preferable if fast enough; use `store: new (require('express-rate-limit').MemoryStore)()` per test to reset between suites.
    3. Use `supertest(app)` — create a minimal Express app in the test file that mounts only the chatbot router with a stubbed auth middleware that populates `req.user = { id: 'test-user-1', role: 'patient' }`.
    4. For the emergency test: mock Anthropic to yield `{ type: 'content_block_delta', delta: { type: 'text_delta', text: 'Call 911 now. <triage>{"urgency":"emergency","specialties":[],"summary":"chest pain","ready_for_referral":false}</triage>' } }` then finish. Assert the final SSE `done` event JSON contains `"emergency":true` and `"doctors":[]`.
    5. For the timeout test: mock Anthropic to return a never-resolving async iterable. Use `jest.useFakeTimers()` to fast-forward past 25s. Assert an error SSE event is written.
    6. For rate limit: create the app with a fresh chatbotLimiter instance; loop 31 requests; assert the 31st response status is 429.
    7. Full suite MUST pass: `cd apps/api && npx jest --testPathPattern='chatbot|sessionStore|triageParser'` — 0 failures.
    8. If `doctorRanking.test.js` requires a live MongoDB and one is not available in CI, mark those specific tests with `it.skip(...)` and add a TODO comment referencing Wave 4 QA (they must be verified manually then).
  </action>
  <verify>
    <automated>cd apps/api && npx jest --testPathPattern='chatbot|sessionStore|triageParser' --passWithNoTests 2>&1 | tee /tmp/jest-p9.log | grep -E "Tests:.*passed" && ! grep -qE "Tests:.*[1-9][0-9]* failed" /tmp/jest-p9.log</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/api && npx jest --testPathPattern='chatbot|sessionStore|triageParser' --passWithNoTests 2>&1 | grep -cE "Tests:.*passed"` returns >=1
    - `cd apps/api && npx jest --testPathPattern='chatbot|sessionStore|triageParser' --passWithNoTests 2>&1 | grep -cE "Tests:.*[1-9][0-9]* failed"` returns 0
    - `grep -c "it\\(\\|test\\(" apps/api/src/routes/__tests__/chatbot.test.js` returns >=8 (covers all listed behaviors)
    - `grep -c "jest.mock.*@anthropic-ai/sdk\\|@anthropic-ai/sdk.*jest.fn\\|jest.mock.*anthropic" apps/api/src/routes/__tests__/chatbot.test.js` returns >=1
    - `grep -cE "429|rate.?limit" apps/api/src/routes/__tests__/chatbot.test.js` returns >=1
    - `grep -c "emergency" apps/api/src/routes/__tests__/chatbot.test.js` returns >=1
    - `grep -c "supertest\\|request(" apps/api/src/routes/__tests__/chatbot.test.js` returns >=1
    - `grep -c "clearSession\\|session.*reset\\|DELETE.*session" apps/api/src/routes/__tests__/chatbot.test.js` returns >=1
    - `grep -c "useFakeTimers\\|jest.advanceTimersByTime" apps/api/src/utils/__tests__/sessionStore.test.js` returns >=1 (TTL test uses fake timers)
  </acceptance_criteria>
  <done>All chatbot Jest suites green; rate limit, emergency, timeout, session reset behaviors covered by automated tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PHI-in-logs audit + HIPAA-AUDIT.md + BAA gate finalization</name>
  <files>.planning/phases/09-ai-patient-chatbot/HIPAA-AUDIT.md, .planning/phases/09-ai-patient-chatbot/BAA-STATUS.md, apps/api/src/routes/chatbot.js, apps/api/src/services/chatbotService.js, apps/api/src/utils/sessionStore.js</files>
  <read_first>
    - apps/api/src/routes/chatbot.js (grep for any console.* that touches PHI)
    - apps/api/src/services/chatbotService.js
    - apps/api/src/utils/sessionStore.js
    - .planning/phases/09-ai-patient-chatbot/BAA-STATUS.md (from 09.1)
    - .planning/phases/09-ai-patient-chatbot/09-RESEARCH.md#pitfall-1-phi-leakage
    - .planning/phases/09-ai-patient-chatbot/09-RESEARCH.md#security-domain
    - CLAUDE.md (HIPAA/GDPR project rules)
  </read_first>
  <action>
    1. **Static audit — enforce zero PHI logging**:
       - Run: `grep -rnE "console\\.(log|warn|error|info)" apps/api/src/routes/chatbot.js apps/api/src/services/chatbotService.js apps/api/src/utils/sessionStore.js apps/api/src/utils/triageParser.js apps/api/src/utils/doctorRanking.js`
       - For every match: verify the logged fields are ONLY: requestId, userId, urgency (classification), durationMs, tokenCount, error type name, doctorsReturned count. Nothing else.
       - If any log line references `req.body.message`, `content`, `history`, `text`, `symptomText`, `accumulated`, or any variable containing raw user message content: REMOVE the reference. Log the non-PHI metadata only.
       - Also verify: no log statement contains `err.stack` if `err` could carry accumulated response text (Anthropic errors sometimes include body snippets). Use `err.name` + `err.message`, and truncate `err.message` to 200 chars if in doubt.

    2. **Write HIPAA-AUDIT.md** at `.planning/phases/09-ai-patient-chatbot/HIPAA-AUDIT.md` with this exact structure:
       ```markdown
       # Phase 9 HIPAA Audit — AI Patient Chatbot

       **Audit date:** {today}
       **Auditor:** {developer}
       **Scope:** apps/api/src/routes/chatbot.js, apps/api/src/services/chatbotService.js, apps/api/src/utils/sessionStore.js, apps/api/src/utils/triageParser.js, apps/api/src/utils/doctorRanking.js

       ## PHI Touchpoint Inventory

       | Touchpoint | Contains PHI? | Mitigation | Verified By |
       |-----------|--------------|-----------|-------------|
       | Incoming POST /api/chatbot/message body.message | YES (symptom text) | Server-side validation limits size; never logged; passed only to Anthropic (BAA required) + in-memory session | grep gate: no `req.body.message` in any log line |
       | In-memory session store | YES | node-cache in-process only; TTL 30 min; MAX 20 turns; not persisted to disk; process restart wipes | Code review of sessionStore.js — no fs/db write |
       | Anthropic Claude API request payload | YES | HTTPS transport; server-side API key; BAA required (see BAA-STATUS.md) | BAA-STATUS.md |
       | SSE response body streamed to client | YES | Direct pipe to authenticated patient; TLS in production; not stored server-side | Manual smoke test |
       | GET /api/chatbot/doctors response | NO (public doctor info) | Standard doctor listing; no patient PHI included | Response schema review |
       | Server logs (console output) | NO (design invariant) | Grep gate below | Automated grep in this audit |

       ## Log Content Verification

       Grep-based invariant checks performed on this audit date:

       - PHI markers scanned for: `req.body.message`, `history`, `content`, `text`, `accumulated`, `symptomText`
       - Grep command: `grep -rnE "console\\.(log|warn|error|info)" apps/api/src/routes/chatbot.js apps/api/src/services/chatbotService.js apps/api/src/utils/sessionStore.js`
       - Expected: every match logs ONLY non-PHI metadata (requestId, userId, urgency, durationMs, tokenCount, error type/name, doctorsReturned count).
       - Result: {PASS | FAIL with specific line numbers}

       ## Access Control

       | Control | Location | Verified |
       |---------|----------|----------|
       | JWT auth required | apps/api/src/routes/chatbot.js — auth middleware on all routes | ✓ |
       | Patient role required | requireRole('patient') on all chatbot routes | ✓ |
       | Per-user rate limit | chatbotLimiter keyed by req.user.id | ✓ |
       | Session isolation | sessionStore keyed by req.user.id | ✓ |

       ## Data Retention

       | Data | Retention | Erasure |
       |------|-----------|---------|
       | Conversation history | 30 min sliding TTL, in-memory | Automatic (TTL) + DELETE /api/chatbot/session |
       | Anthropic-side data | Per Anthropic BAA / retention policy | Anthropic BAA governs |
       | Server logs | Standard app log retention (Railway logs, ~7 days) | Standard log rotation |

       ## Sign-off

       - [ ] All log lines verified as non-PHI (grep gate PASS)
       - [ ] BAA-STATUS.md reads Status: SIGNED before production launch
       - [ ] Rate limiter and role gates verified via Jest suite
       - [ ] Data retention documented above matches implementation

       **This audit MUST be re-run whenever chatbot.js, chatbotService.js, or sessionStore.js is modified.**
       ```

    3. **Update BAA-STATUS.md** — if BAA is confirmed signed with the project owner, change Status field to `SIGNED` and record the sign date. If unknown, leave as UNKNOWN and add an inline warning at the top: `⚠ PRODUCTION LAUNCH BLOCKED until BAA is signed.`

    4. Run the grep gate one more time as verification. If any log line references PHI fields, FAIL — go back and remove the offending log statement.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-ai-patient-chatbot/HIPAA-AUDIT.md && test -f .planning/phases/09-ai-patient-chatbot/BAA-STATUS.md && grep -qE "Status:.*(SIGNED|UNKNOWN|PENDING)" .planning/phases/09-ai-patient-chatbot/BAA-STATUS.md && ! grep -rnE "console\.(log|warn|error|info).*(req\.body\.message|history|accumulated\b)" apps/api/src/routes/chatbot.js apps/api/src/services/chatbotService.js apps/api/src/utils/sessionStore.js 2>/dev/null | grep -v "// noqa" | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/09-ai-patient-chatbot/HIPAA-AUDIT.md` exits 0
    - `grep -c "PHI Touchpoint Inventory\\|Log Content Verification\\|Access Control\\|Data Retention" .planning/phases/09-ai-patient-chatbot/HIPAA-AUDIT.md` returns >=4
    - `grep -c "Sign-off" .planning/phases/09-ai-patient-chatbot/HIPAA-AUDIT.md` returns >=1
    - `grep -cE "Status:\\s*(SIGNED|UNKNOWN|PENDING)" .planning/phases/09-ai-patient-chatbot/BAA-STATUS.md` returns >=1
    - **CRITICAL GATE:** `grep -rnE "console\\.(log|warn|error|info)" apps/api/src/routes/chatbot.js apps/api/src/services/chatbotService.js apps/api/src/utils/sessionStore.js | grep -vE '^[^:]+:[^:]+:[[:space:]]*(//|\\*)' | grep -E "req\\.body\\.message|\\baccumulated\\b|\\bhistory\\b(?![Snapshot])" | wc -l` returns 0 (no PHI in non-comment log lines)
    - `grep -rE "\\.stack" apps/api/src/routes/chatbot.js apps/api/src/services/chatbotService.js | wc -l` returns 0 (or if used, wrapped in a safe non-PHI extraction — reviewer confirms)
  </acceptance_criteria>
  <done>HIPAA audit document exists with PHI inventory + sign-off checklist. BAA status is documented (SIGNED or explicitly UNKNOWN). Zero PHI in any log statement — verified by grep.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Cross-platform end-to-end smoke test + QA checklist sign-off</name>
  <what-built>
    - Full Jest suite for chatbot modules (Task 1)
    - HIPAA audit document with PHI verification (Task 2)
    - BAA-STATUS.md finalized
    - Rate limit, emergency, timeout automated tests
  </what-built>
  <how-to-verify>
    1. Ensure API, mobile, and web builds are all running against the same API endpoint (staging URL preferred).

    2. Create `.planning/phases/09-ai-patient-chatbot/QA-CHECKLIST.md` with the following structure and check off each item as you verify:

       ```markdown
       # Phase 9 QA Checklist — {date} — {tester}

       ## API (Wave 1)
       - [ ] Jest suite green: `cd apps/api && npx jest --testPathPattern='chatbot|sessionStore|triageParser'`
       - [ ] Manual curl SSE test streams events (delta → done → [DONE])
       - [ ] Non-patient JWT returns 403
       - [ ] No JWT returns 401
       - [ ] Empty body returns 422
       - [ ] Rate limit fires at 31st request in 1 hour (same user)
       - [ ] AbortController timeout: use `ANTHROPIC_API_KEY=invalid` or throttle network → connection closes gracefully within 26s
       - [ ] Logs contain requestId, userId, urgency, durationMs — NO message content

       ## Mobile (Wave 2)
       - [ ] FAB visible on all patient tabs
       - [ ] FAB tap opens ChatbotScreen modal
       - [ ] Streaming response renders token-by-token
       - [ ] Urgency badge appears after triage
       - [ ] Emergency prompt (e.g., "severe chest pain") → 911 CTA + tel:911 works, NO doctor cards
       - [ ] Non-emergency (e.g., "mild headache") → ranked doctor cards appear
       - [ ] Tapping doctor card navigates to DoctorProfile
       - [ ] Reset button clears state + calls DELETE /api/chatbot/session
       - [ ] Markdown renders (bold, lists)
       - [ ] No `response.body undefined` runtime error

       ## Web (Wave 3)
       - [ ] Floating bubble visible on all patient pages
       - [ ] Bubble click opens sidebar panel
       - [ ] Escape key closes panel
       - [ ] Streaming response renders token-by-token
       - [ ] Urgency badge appears
       - [ ] Emergency prompt → red banner + tel:911 link, NO doctor cards
       - [ ] Non-emergency → ranked doctors appear
       - [ ] Doctor card click navigates to /patient/doctors/:id
       - [ ] Reset clears state + calls DELETE /api/chatbot/session
       - [ ] Markdown renders (react-markdown + remark-gfm)
       - [ ] Network tab confirms Content-Type: text/event-stream

       ## Compliance (Wave 4)
       - [ ] HIPAA-AUDIT.md exists and sign-off checklist is complete
       - [ ] BAA-STATUS.md reads Status: SIGNED (production gate) — OR explicitly documented as launch blocker
       - [ ] Grep gate for PHI in logs: PASS
       - [ ] No conversation content written to MongoDB (grep chatbot.js + chatbotService.js for `mongoose|Model.create|save\\(`)

       ## Session TTL Behavior
       - [ ] Send message; wait 31 minutes; send another → second message has no context (session evicted)
       - [ ] Send 20+ messages in one session → history capped at 20 (verify by inspecting server memory or reset+resend)

       ## Sign-off
       Tester: {name}
       Result: {PASS | FAIL — with notes}
       Date: {date}
       ```

    3. Walk through EVERY checkbox on both mobile and web. Any FAIL → describe in the "notes" section and either fix on the spot (small) or return to earlier plan (large).

    4. Confirm the API grep gate one more time:
       ```
       grep -rnE "console\\.(log|warn|error|info)" apps/api/src/routes/chatbot.js apps/api/src/services/chatbotService.js apps/api/src/utils/sessionStore.js | grep -vE "//" | grep -E "message|content|history|accumulated"
       ```
       Expected: no matches (or every match is a safe field like `err.message` or `message:` literal string).

    5. If any FAIL cannot be resolved: DO NOT approve. Return to the failing wave's plan and open a gap-closure follow-up.
  </how-to-verify>
  <resume-signal>Type "approved" if every checkbox is checked, the QA-CHECKLIST.md is committed, HIPAA audit sign-off is complete, and BAA gate is either SIGNED or explicitly flagged. Otherwise report the FAIL items with file + reproduction steps.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Same as Waves 1-3. Wave 4 is verification — no new boundaries introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.4-01 | Information Disclosure | Undiscovered PHI leak in logs | mitigate | Grep gate in Task 2 acceptance_criteria; re-run before every code change per HIPAA-AUDIT.md rule |
| T-09.4-02 | Repudiation | Ship without BAA and later find PHI processing violated HIPAA | mitigate | BAA-STATUS.md is a documented pre-production gate; QA-CHECKLIST requires SIGNED for prod launch |
| T-09.4-03 | Denial of Service | Rate limit could be bypassed via multiple accounts | accept | Per-user rate limit is the intended contract; abuse via account creation is throttled by registerLimiter (existing) |
| T-09.4-04 | Tampering | Regression to PHI logging in a future edit | mitigate | HIPAA-AUDIT.md marked as "MUST be re-run whenever chatbot.js is modified"; grep gate can be added to CI |
</threat_model>

<verification>
- `cd apps/api && npx jest --testPathPattern='chatbot|sessionStore|triageParser'` → 0 failures
- QA-CHECKLIST.md exists with all boxes checked or explicit FAIL notes
- HIPAA-AUDIT.md sign-off section complete
- BAA-STATUS.md Status is SIGNED (or documented blocker)
- Grep gate for PHI in logs: 0 matches on non-comment lines
</verification>

<success_criteria>
- All automated tests pass across chatbot, sessionStore, triageParser, doctorRanking suites
- HIPAA audit document committed and signed off
- BAA compliance status documented (SIGNED for prod launch OR blocker flagged)
- QA checklist covers API + mobile + web + compliance + TTL — all items verified
- Rate limit + emergency + timeout behaviors have automated coverage
- Zero PHI in any log statement (grep-verified)
- Phase 9 is production-ready (or explicit blockers listed with owners)
</success_criteria>

<output>
After completion, create `.planning/phases/09-ai-patient-chatbot/09.4-SUMMARY.md` documenting:
- Test coverage matrix (which REQ maps to which passing test)
- HIPAA audit result (PASS / FAIL with details)
- BAA gate status
- Cross-platform QA results
- Any deferred items or follow-up gaps
- Production readiness statement (READY / BLOCKED with reasons)
</output>
