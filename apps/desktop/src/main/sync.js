'use strict';
// Wave 2 agent (Task 3) will implement pull + push-queue sync engine
module.exports = {
  start:        () => {},
  triggerSync:  async () => {},
  getStatus:    () => ({ status: 'offline', lastSyncAt: null }),
};
